name: techtim
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # Web framework
      - fastapi>=0.109.0
      - uvicorn[standard]>=0.27.0
      - websockets>=12.0
      - python-multipart>=0.0.6
      
      # Data validation
      - pydantic>=2.5.0
      - pydantic-settings>=2.1.0
      
      # HTTP client
      - httpx>=0.26.0
      
      # Environment management
      - python-dotenv>=1.0.0
      
      # CLI interface
      - rich>=13.7.0
      - typer>=0.9.0
      
      # LLM clients - GlueLLM unified interface
      - ollama>=0.1.6
      - git+https://github.com/Bioto/glue-llm.git
      
      # Testing
      - pytest>=7.4.0
      - pytest-asyncio>=0.23.0
      - pytest-cov>=4.1.0

